server:
  hostname: localhost

connectors:
  http:
    protocol: HTTP/1.1
    port: 8081

webapps:
  instances:
     sample:
       context: sample
       warFile: sample.war

     chat:
       context: chat
       warFile: chat.war

